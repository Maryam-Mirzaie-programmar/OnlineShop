@model Datalayer.Feature

@{
    ViewBag.Title = "Delete";
}

<p>آیا از حذف این آیتم مطمئن هستید؟</p>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.FeatureTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FeatureTitle)
        </dd>

    </dl>

    @using (Ajax.BeginForm("Delete", "Features", FormMethod.Post, ajaxOptions: new AjaxOptions
    {
        OnSuccess = "successAjax",
        UpdateTargetId = "featuresList"
    }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="حذف" class="btn btn-danger" />
        </div>
    }
</div>
